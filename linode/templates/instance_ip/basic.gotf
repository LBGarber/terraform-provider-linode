{{ define "instance_ip_basic" }}

resource "linode_instance" "{{.InstanceLabel}}" {
    label = "{{.InstanceLabel}}"
    group = "tf_test"
    type = "g6-nanode-1"
    region = "us-east"
    disk {
        label = "disk"
        image = "linode/alpine3.11"
        root_pass = "b4d_p4s5"
        authorized_keys = ["{{.PubKey}}"]
        size = 3000
    }
}

resource "linode_instance_ip" "test" {
    linode_id = linode_instance.{{.InstanceLabel}}.id
    public = true
}

{{ end }}