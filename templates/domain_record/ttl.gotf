{{ define "domain_record_ttl" }}

{{ template "domain_basic" . }}

resource "linode_domain_record" "foobar" {
    domain_id = "${linode_domain.foobar.id}"
    name = "{{.RecordName}}"
    record_type = "CNAME"
    target = "{{.RecordName}}.{{.Domain}}"
    ttl_sec = {{.TTLSec}}
}

{{ end }}